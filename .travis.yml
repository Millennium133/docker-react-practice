sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t Millennium/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true Millennium/react-test npm test

after_success:
  - docker build -t Millennium/multi-client ./client
  - docker build -t Millennium/multi-nginx ./nginx
  - docker build -t Millennium/multi-server ./server
  - docker build -t Millennium/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push Millennium/multi-client
  - docker push Millennium/multi-nginx
  - docker push Millennium/multi-server
  - docker push Millennium/multi-worker
